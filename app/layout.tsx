import type React from "react"
import "./globals.css"
import { Inter } from "next/font/google"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "Feed Focus - Organisez votre expérience LinkedIn",
  description:
    "Organisez et filtrez votre expérience LinkedIn en vous concentrant sur des profils et contenus spécifiques",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
